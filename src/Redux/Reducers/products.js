const initialState = {
		list: require('../../Jsons/productsList.json'),
};

export const reducerProductsList = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};